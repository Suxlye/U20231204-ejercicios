function contarCaracteres() {
    let cadena = prompt("Por favor, ingresa una cadena:");
    let numeroDeCaracteres = cadena.length;
    alert("La cadena tiene " + numeroDeCaracteres + " caracteres.");
}
