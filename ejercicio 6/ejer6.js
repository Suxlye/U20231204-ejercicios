(function() {
    var frase = prompt("Ingresa una frase:");
    var palabras = frase.split(" ");
    var numDePalabras = palabras.length;
    alert("La frase tiene " + numDePalabras + " palabras.");
})();
